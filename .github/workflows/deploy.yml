name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Deployment will be triggered automatically by Render"
          echo "Visit your Render dashboard to monitor deployment"
