-- Optional SQL seed for a live Supabase project.
-- Prefer src/lib/seed/data.ts for local demo browsing.
-- Replace image URLs and booking links with real data before production.

-- Example: insert destinations (run after migrations; requires manually created auth users for videos).
-- This file is intentionally minimal — TypeScript seed powers Iteration 1 offline.

select 'Seed the app via src/lib/seed/data.ts for local browsing.' as note;
